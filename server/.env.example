# CCU3 Connection Settings
# The IP address or hostname of your CCU3
# Default: localhost (for running on CCU3)
# Set to CCU's IP when running externally (e.g., 192.168.178.26)
CCU_HOST=192.168.178.26

# CCU3 Authentication (required for remote connections)
# Leave empty when running on CCU3 (uses local port 8183 without auth)
# Required when running externally (uses port 8181 with auth)
CCU_USER=Admin
CCU_PASS=your-password-here

# The IP address where the RPC server should listen for CCU events
# Default: 127.0.0.1 (for running on CCU3)
# Set to your PC's IP when running externally (e.g., 192.168.178.50)
CALLBACK_HOST=192.168.178.50

# The port for the RPC server (receives events from CCU)
# Default: 9099
RPC_SERVER_PORT=9099
