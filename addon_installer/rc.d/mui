#!/bin/sh

ADDON_DIR="/usr/local/addons/mui"

case "$1" in
    info)
        echo "MUI Addon"
        echo "Verzeichnis: $ADDON_DIR"
        ;;
    uninstall)
        echo "Deinstalliere MUI Addon..."
        rm -rf $ADDON_DIR
        echo "Deinstallation abgeschlossen."
        ;;
    *)
        echo "Usage: $0 {info|uninstall}"
        exit 1
        ;;
esac

exit 0